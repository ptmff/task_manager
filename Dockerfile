# Используем официальный образ Node.js (подходит версия, например, 16)
FROM node:22

# Задаём рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# Копируем файлы package.json и package-lock.json
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем все остальные файлы в контейнер
COPY . .

# Открываем порт, который использует ваше приложение (предположим, 3000)
EXPOSE 3000

# Запускаем сервер
CMD ["node", "server.js"]
